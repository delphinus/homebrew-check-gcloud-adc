{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "case \"$TOOL_INPUT\" in *'gh release create'*|*'gh release upload'*) echo 'ERROR: ローカルからのリリースは禁止されています。git tag vX.Y.Z && git push origin vX.Y.Z でタグを push し、CI に任せてください。' >&2; exit 1;; esac"
          }
        ]
      }
    ]
  }
}
